# Prompts used with AI tools

1. "Generate a FastAPI project structure for a product detail API with local JSON persistence and include tests and README." 
2. "Write a FastAPI endpoint that returns product details by id, reading from a JSON file, with proper error handling and Pydantic schemas." 
3. "Create pytest tests for the endpoint using TestClient that mock the JSON file contents." 